# CKKS API Configuration
# For local development, use localhost:3001
# For production, use relative path /api (same domain)
CKKS_API_URL=http://localhost:3001

# External API (legacy - still used as fallback)
API=https://api.ckks.pl/api

# Next.js Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NODE_ENV=development