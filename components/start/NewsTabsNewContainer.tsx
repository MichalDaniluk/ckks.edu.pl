import NewsTabsNew from '@components/start/NewsTabsNew';

const NewsTabsNewContainer = ({news}) => {
	return (
		console.log(news)
	);
//  return news && <NewsTabsNew news={news} />
};

export default NewsTabsNewContainer;
